[project]
name = "general-agent-template"
version = "0.1.0"
description = "Minimal CLI nanobot for learning agent core concepts"
requires-python = ">=3.11"
license = {text = "MIT"}
dependencies = [
    "typer>=0.9.0",
    "rich>=13.0.0",
    "openai>=1.0.0",
    "python-dotenv>=1.0.0",
    "fastapi>=0.110.0",
    "uvicorn[standard]>=0.29.0",
]

[project.scripts]
agent = "cli.commands:app"
agent-api = "api.main:run"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["agent", "api", "bus", "cli", "cron", "heartbeat", "providers", "session"]
